# BUCK file for fc-agent guest binary

# Guest agent binary
rust_binary(
    name = "fc-agent",
    srcs = glob(["src/**/*.rs"]),
    crate_root = "src/main.rs",
    edition = "2021",
    deps = [],
)
